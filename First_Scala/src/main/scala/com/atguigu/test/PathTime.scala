package com.atguigu.test

import java.time.format.DateTimeFormatter
import java.time.{LocalDate, LocalDateTime}

object PathTime {


  def getPathTime (num:Int,Time:String)={
//    var Time = LocalDateTime.now()
//    for(i<-1to num)yield {
//      var Time2=Time.plusDays(-num)
//      var TimePath=Time2.format(DateTimeFormatter.ofPattern("yyyyMMdd"))
//      ${TimePath}
//    }
  }
}
